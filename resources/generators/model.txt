(ns models.__table
  (:require [db.__table])
  (:refer-clojure :exclude [find update]))

(defn all []
  (db.__table/query))

(defn find [m]
  (db.__table/find m))

(defn create [m]
  (db.__table/insert m))

(defn update [m]
  (db.__table/update m))

(defn delete [m]
  (db.__table/delete m))
